# Web framework
flask
flask-cors
flask-sqlalchemy

# Database
sqlalchemy
psycopg2-binary

# Environment
python-dotenv

# Authentication
PyJWT>=2.8.0
bcrypt>=4.0.0

# Transcription + Extraction (online — Groq API)
groq>=0.9.0

# Audio utilities (minimal — just for file handling)
numpy
soundfile

# Ollama fallback (offline extraction — optional)
openai

# Production server
gunicorn
